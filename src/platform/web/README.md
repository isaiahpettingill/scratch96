# Web Platform

Browser-only platform code belongs here.

Examples:

- Downloading `cartridge.elf`.
- Importing project JSON, PNG, and audio files through file inputs.
- Loading WASM artifacts from `public/wasm/`.
- Keyboard and gamepad input capture for preview.
