# Tauri Platform

Tauri-specific platform code belongs here.

Keep this thin. The desktop app should share the website's project model, compiler orchestration, editor components, and WASM runtime adapters.

Examples:

- Native file open/save dialogs.
- Stable project paths.
- Desktop packaging metadata.
- Optional local cache management for vendored WASM artifacts.
